import antd from "antd";
import Button from "./button";
import "./index.scss";

const { ButtonProps, ButtonShape, ButtonType } = antd.Button;
const { ButtonGroupProps } = antd.Button.Group;

export { ButtonProps, ButtonShape, ButtonType };
export { ButtonGroupProps };

export default Button;
